<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <Description>Kontext retrieval SDK — the composable pipeline (plan → search → fuse → stages → cut) and its standard parts: fusers, stages, and relevance models.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Kurrent.Kontext.Contracts\Kurrent.Kontext.Contracts.csproj" />
    <ProjectReference Include="..\Kurrent.Kontext.Pipelines\Kurrent.Kontext.Pipelines.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Google.Protobuf" />
    <PackageReference Include="Microsoft.Extensions.AI.Abstractions" />
  </ItemGroup>

  <ItemGroup>
    <None Remove="Kurrent.Kontext.Retrieval.csproj.DotSettings" />
  </ItemGroup>
</Project>
