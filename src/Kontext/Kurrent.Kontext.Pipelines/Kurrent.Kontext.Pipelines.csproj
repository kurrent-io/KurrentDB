<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <Description>Kontext pipeline core — typed steps whose input/output types enforce stage ordering at compile time, plus the fan-in and cascade combinators the memory pipelines compose with.</Description>
  </PropertyGroup>
</Project>
